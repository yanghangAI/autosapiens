Architectural exploration of code and docs. No production code changed. Comprehensive docs fixes: broken links, stale content, missing navigation, completed issue checkboxes.

## Changes

### `pose/docs/bedlam2/README.md` (new)
- Created navigation hub with quick-links table and recommended reading order.
- Was the missing entry point; readers previously had no clear "start here" doc.

### `pose/docs/README.md`
- Added "Doc Layers" section explaining design/bedlam2/prd/issues hierarchy.
- Added issue 006 link.
- Added bedlam2/README.md link.

### `pose/docs/bedlam2/training.md`
- Fixed stale env: `sapiens_lite` → `sapiens` (sapiens_lite is inference-only).
- Fixed stale data layout: `depth_npy/` → `depth/npy/` and `depth/npz/`.
- Fixed broken internal link: `docs/BEDLAM2_INTEGRATION.md` → `bedlam2/integration.md`.

### `pose/docs/prd/issues/001-003` (broken links)
- Fixed parent PRD path in all three files: `prd_transformer_decoder_head.md` → `prd/transformer_decoder_head.md`.

### `pose/docs/prd/issues/001_transformer_decoder_head_module.md`
### `pose/docs/prd/issues/002_training_config_smoke_test.md`
### `pose/docs/prd/issues/004_restructure_tags.md`
### `pose/docs/prd/issues/005_absolute_mpjpe_and_epoch_avg.md`
- Marked all acceptance criteria as complete (`[x]`); added "Status: COMPLETE" header.

### `pose/docs/prd/issues/003_ab_training_evaluation.md`
- Fixed wrong doc path: `pose/docs/pipeline.md` → `pose/docs/design/pipeline.md`.
- Runs still in progress; checkboxes remain unchecked.

### `pose/docs/design/pipeline.md`
- Split 380-line monolith into 3 focused docs (see below).
- Now covers only: model architecture + inference pipeline + why root-relative.

### `pose/docs/design/data_transforms.md` (new)
- Contains former §1 (data format, coordinate system, depth maps, joint subset, indexing) + §2 (transform chain: NoisyBBox → CropPerson → SubtractRoot → PackBedlamInputs).

### `pose/docs/design/training_loop.md` (new)
- Contains former §4 (optimizer, LR, loss, metrics, TensorBoard tags, checkpointing).

### `pose/CLAUDE.md` and root `CLAUDE.md`
- Updated docs structure trees to reflect new files (bedlam2/README.md, data_transforms.md, training_loop.md, training_results.md, issue 006).
- Updated BEDLAM2 head description: now lists both `Pose3dTransformerHead` (default) and `Pose3dRegressionHead` (baseline).
- Removed "accuracy verification still pending" — both heads have training results.
- Updated key file locations table to include transformer head file.

### `pose/docs/bedlam2/integration.md`
- Added table of contents.
- Replaced duplicated "Training Setup" section (hyperparameters, commands) with pointer to `training.md`.

### `pose/docs/prd/issues/006_bedlam2_transform_testability.md` (new)
- Created RFC for making `bedlam2_transforms.py` unit-testable.
- Root cause: 3 module-level globals (`_label_cache`, `_depth_mmap`, `_depth_npz_cache`) block all isolation testing.
- Proposed fix: encapsulate globals into `_GlobalLabelCache`/`_GlobalDepthCache` singletons, add `_label_cache`/`_depth_cache` injectable params to `LoadBedlamLabels.__init__`, add `make_bedlam2_results(after_stage=...)` fixture factory.
- 0 config changes, 0 behavior changes; 1 production class touched.
